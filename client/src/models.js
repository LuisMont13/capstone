function predictMlbOutcome(team1Stats, team2Stats){
    //Calculate overall scores for each team based on their stats
    const team1Score = calculateOverallScore(team1Stats);
    const team2Score = calculateOverallScore(team1Stats);

}